pipeline {
    agent any

    stages {
        stage('no docker') {
            steps {
                sh '''
                    echo "Hello World"
                '''
            }
        }
    }
}
